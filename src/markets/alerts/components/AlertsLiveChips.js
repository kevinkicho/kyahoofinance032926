/** Live-chip / remount helpers for alerts tiles that can paint empty. */

/** Asset rows the correlation heatmap can .find. Leftover isLive assets bags remount-crash .find. */
export function returnsDataAssets(returnsData) {
  return Array.isArray(returnsData?.assets) ? returnsData.assets : [];
}
