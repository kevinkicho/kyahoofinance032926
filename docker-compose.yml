services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file: .env
    restart: unless-stopped
    volumes:
      # Persist data cache across container restarts
      - datacache:/app/server/datacache

volumes:
  datacache:
