{
  "name": "global-macro-server",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "description": "Optional nested install for local server-only work. App Hosting runs `node server/index.js` after ROOT `npm ci` (express@5 from root). Prefer root deps for production parity.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.0",
    "express": "^5.2.1",
    "node-cache": "^5.1.2",
    "xlsx": "^0.18.5",
    "yahoo-finance2": "^3.14.0"
  },
  "optionalDependencies": {
    "helmet": "^8.0.0"
  }
}
