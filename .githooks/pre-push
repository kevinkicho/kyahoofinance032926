#!/bin/sh
# Block push unless local preflight passes.
# Install: npm run hooks:install  (also runs via npm prepare)
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "pre-push: running npm run preflight…"
npm run preflight
