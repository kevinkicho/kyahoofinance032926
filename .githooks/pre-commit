#!/bin/sh
# Fast commit gate: secrets + workflow policy (not full vitest — that is pre-push).
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "pre-commit: guard:secrets + lint-workflows…"
npm run guard:secrets
node scripts/lint-workflows.mjs
